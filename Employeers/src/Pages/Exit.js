import React from 'react'
import { ScrollView, Text, SafeAreaView, View, Image } from 'react-native';

const Exit = () => {

    return(
        <View>
        <Text>Exit page</Text>
    </View>
    )
}

export default Exit;